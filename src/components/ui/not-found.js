import Typography from "@mui/material/Typography";
import Box from "@mui/material/Box";

const NotFound = () => {
  return (
    <Box>
      <Typography variant="h1">Không thể tìm thấy trang của bạn</Typography>
    </Box>
  );
};

export default NotFound;
